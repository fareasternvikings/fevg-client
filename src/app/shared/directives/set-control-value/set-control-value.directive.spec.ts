import {SetControlValueDirective} from './set-control-value.directive'

describe('SetControlValueDirective', () => {
  it('should create an instance', () => {
    const directive = new SetControlValueDirective()
    expect(directive).toBeTruthy()
  })
})
